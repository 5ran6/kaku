class Orders {
  final String date;
  final String to;
  final String address;
  final bool status;
  final String when;
  final int level;

  Orders(
      {this.date, this.to, this.address, this.status, this.when, this.level});
}
