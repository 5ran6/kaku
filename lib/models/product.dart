class Product {
  final String date;
  final String to;
  final String address;
  final bool status;
  final String when;
  final int level;

  Product(
      {this.date, this.to, this.address, this.status, this.when, this.level});
}
